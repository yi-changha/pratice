#include <stdio.h>

int main() {
	int a = 2; // 초기화
	int b = 3;

	printf("%d + %d = %d\n", a, b, a + b);

	// 변수 이름 : 알파벳 대소문자, _, 숫자(첫번째 글자)

	int hap = a + b;

	printf("%d + %d = %d\n", a, b, hap);
}